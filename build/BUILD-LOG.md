# Build log

Overnight build of Content Engine from `spec/`, milestone by milestone. Each entry: what was built, how the "done when" was verified, and deviations from spec.

## M0 — Walking skeleton (2026-08-12)

**Built:** Full `build/` scaffold per `spec/structure.md`. One Bun process: Hono API on `127.0.0.1:4321`, `bun:sqlite` DB at `data/content-engine.db` with idempotent numbered migrations (`PRAGMA user_version`) applying the full schema from `schema.sql` and seeding default settings (30-min interval, both starter prompts, voice count 5). `scheduler.ts` with `runOnce()` (per-source isolation, 3-attempt retries with backoff, attempt-trace recording into `run_sources` — retries are M1 scope but were cheap to include at birth), RSS/Atom fetcher on `fast-xml-parser` with dedupe by `(source_id, external_id)` and the "initial import" rule (first fetch of a new source marks everything `skipped`/"initial import"). Source CRUD with per-type validation (RSS validated by fetching once, feed title becomes display name; HN singleton; X handle normalization; YouTube handle→channel_id resolution — resolution lives in `fetchers/youtube.ts` since sources CRUD needs it, full YouTube milestone comes later). React/Vite SPA: five routes, design tokens (three-state light/dark from the styleguide), Nav/Card/Chip/Button/Field components, functional Inbox (filter chips, cards, empty state, Run check now) and Sources pages, functional Runs list (fleshed out in M1), Editor and Settings shells. API-unreachable banner. `bun run dev` = server + Vite with `/api` proxy; `bun run build` + `bun run start` = single-process serving of `web/dist`.

**Verified done-when:** `bun run dev` boots both processes (checked the Vite proxy answers `/api/settings` on :5173 and the server on :4321). Added `https://hnrss.org/frontpage` as an RSS source over the API → POST `/api/runs/trigger` → run #1 recorded 20 new entries; `GET /api/entries` shows them with `state=new` (visible in the Inbox, which lists all non-dismissed entries until the filter exists). Second trigger deduped to 0 new. Killed and restarted the server — entries, source, and runs all intact (SQLite WAL).

**Deviations (M0):** (1) Retry/backoff logic landed in M0's `runOnce()` rather than waiting for M1 — no behavior difference for M0's happy path. (2) Added `source_type`/`source_label` denormalized columns on `entries` and a `channel_id` column on `sources` (plans call for both behaviors; the core schema list just doesn't spell out the columns). (3) Runs totals stored as three columns (`new_count`/`matched_count`/`failed_count`) instead of an opaque "totals" blob. (4) Added `web/src/styles/app.css` next to `tokens.css` for shared component styles — structure.md only names `tokens.css`; keeping raw tokens separate from component CSS seemed truer to its intent. (5) `bun` was not installed on this machine — installed Bun 1.3.14 (plus `unzip`) system-side; nothing in the repo depends on that beyond `bun` being on PATH.

## M1 — Scheduler, run history, HN (2026-08-12)

**Built:** `Bun.cron` registration from the interval setting (`registerSchedule()` — stops the old handle and registers a new one when the setting changes; wired into boot and the settings PUT). Interval→cron mapping includes a hidden `1m` test value not offered in the UI. `nextRunAt()` via `Bun.cron.parse()` surfaces "next check at HH:MM" in the Inbox header and the Runs empty state. HN front-page fetcher (Firebase API, top-30 slice, item fetches in parallel, one missing item never fails the page; content = points + comment count + article URL; entry URL = the HN discussion page to match the mockup's "Open on HN"). Runs page: newest-first cards with expandable per-source table, attempt counts, error trace block, and plain-language hints (407→Floxy settings link, "not configured"→Settings, claude→CLI hint) with 4s polling while open. 30-day pruning runs at boot (from M0). Retries/backoff/attempt traces were already in from M0.

**Verified done-when:** Set `check_interval=1m` (test value) directly in SQLite, restarted, added the HN source plus a deliberately dead feed URL (`hnrss.org/this-feed-does-not-exist-404`, inserted via SQL since add-time validation correctly refuses it). Runs #3 (13:53) and #4 (13:54) fired unattended with `trigger=cron`. Run #3 shows the broken source `failed` after 3 attempts with the human-readable trace ("attempt 1 13:53:00 → Feed fetch failed: HTTP 404 Not Found … giving up after 3 attempts") while `hn:frontpage` (30 new, initial import) and the live feed succeeded in the same run — per-source isolation confirmed.

**Deviations:** `Bun.cron` in Bun 1.3.14 is minute-granularity (5-field) and its in-process form takes `(expression, handler)` — no title. Run-status semantics pinned as: source `ok` (first try) / `retrying` (succeeded after retries, surfaces as amber) / `failed`; run chip shows crimson "n failed" > amber "Retries" > green OK. The mockup was ambiguous here (showed OK with attempts=2).

## M2 — Settings & credentials (2026-08-12)

**Built:** Full Settings page per its mockup — Schedule (interval select), Telegram (bot token + chat ID, "Send test message"), Floxy proxy (host/port/username/password, "Test connection"), Twitter CLI (both cookies, "Test auth"), both prompt textareas, voice-examples-count select, sticky save bar that names the dirty sections and only sends edited keys. Secret handling: stored in SQLite; GET `/api/settings` returns `{set: bool}` per secret, never values; password inputs show a masked placeholder when set and send only when actually edited. Numeric validation for chat ID and port; interval saves re-register cron live. Server modules: `notify/telegram.ts` (plain-text sendMessage — no parse_mode, so titles never need escaping), `proxy/floxy.ts` (fresh `ses_xxxxxx` session id per call encoded as `<username>-session-<id>`; the ONLY file that knows the scheme), `fetchers/twitter.ts` (twitter-cli subprocess, creds via env only — never argv — with expired-cookie detection), and `routes/test.ts` wiring the three test buttons to real checks.

**Verified done-when:** Settings (including secrets) persist across a server restart; GET shows presence flags only. Real checks confirmed as real: a fake Telegram token produced Telegram's own "Unauthorized" API error; a fake proxy host produced a connection failure naming the fresh session id; missing creds produce "not configured — add it in Settings" pointers. Fixed a bug found while testing: the twitter test wrongly reported "CLI not found" when credentials were merely unset.

**Needs real credentials to test (overnight limitation):** the green paths — actual Telegram delivery, a Floxy 407-vs-200 distinction against the real gateway (the exact session-username encoding is an assumption pinned in `proxy/floxy.ts`), and twitter-cli auth (the CLI itself isn't installed here; the exact subcommand is still the spec's open question, pinned during M6).

## M3 — Taste filter & Telegram (2026-08-12)

**Built:** `llm/claude.ts` — the only place `claude -p` is spawned; full prompt via stdin (argv is size-limited and visible in `ps`), 60s timeout (120s for generation), kill-on-timeout, one fresh-call retry on unparseable output, and a distinct `ClaudeUnavailableError` for CLI-missing/logged-out. `llm/filter.ts` — taste prompt from settings wrapped in a code-owned MATCH/SKIP one-line contract. Filter pass inside `runOnce()` after fetching: serial over every pending entry including leftovers from previous runs; per-entry parse failure leaves the entry pending; `ClaudeUnavailableError` or 3 consecutive errors aborts the pass and records a run-level error (new `runs.error_text` column via additive migration 2 — exercising the migration path). Notify pass: Telegram message per matched entry (plain text: title / source / filter's take / URL), strictly on the `new→notified` transition so a refilter can never re-notify; send failure recorded on the run and the entry stays `new` for a resend next run. Inbox now shows matched-only entries (per mockup), with the "All caught up" empty state showing "N sources checked, M entries filtered" from the last run. Runs page renders run-level errors with the claude/Telegram hints.

**Verified done-when:** Live run filtered real pending HN/RSS entries — verdicts stored with one-line reasons (e.g. "The Ultimate Horse" → SKIP "no connection to AI economics…"). Seeded a taste-matching entry: MATCH with a specific reason, `matched_count` attributed to the run. Filter-failure path: restarted the server with `claude` off PATH → run #12 recorded exactly "claude -p failed — is the CLI installed and logged in?" and entries stayed `pending`; after restoring PATH, run #13 re-filtered the same entry to `matched` — nothing lost. Telegram-failure path: with no token configured the run records "Telegram send failed … not configured — add the bot token and chat ID in Settings" and the entry stays `state=new`.

**Needs real credentials to test:** actual Telegram delivery of a match (message content/format) and the `new→notified` transition on success. Everything up to the send — including the resend-next-run behavior — is exercised.

## M4 — Thread studio (2026-08-12)

**Built:** `llm/generator.ts` — composes the `claude -p` input as: generation prompt (settings) → voice examples (last N posted finals, newest first; section omitted entirely when the pool is empty) → item material (title/URL/content, plus transcript for YouTube with beginning+middle-sample truncation at ~15k words) → JSON-array output contract in code. `composeGenerationPrompt()` is exported separately so the exact prompt is inspectable. `routes/threads.ts`: POST `/api/entries/:id/draft` (retries the transcript fetch for YouTube entries missing one, generates, then replaces-or-creates the thread row only AFTER a successful parse — a failed regenerate never loses the previous draft), debounced PUT for edits, `posted` (current edited text → `final_text`, entry → `posted`), and `unposted` (the spec's cheap-undo open question — included since trivial). Editor page per its mockup: breadcrumb, serif title, left column with filter's take + source material (transcript in a scrollable mono block with word count when present), right column of auto-sizing tweet blocks (`field-sizing: content`, 120px min-height), n/total labels, live char counts flipping crimson-bold over 280 (visual only), + Add tweet, Regenerate behind a confirm, Copy all (blank-line separated, no numbering), green Mark as posted with its caption, voice note, inline generation-error card with Retry. Inbox "Draft thread" navigates with `?draft=1` so generation starts on arrival.

**Verified done-when:** Matched entry #491 generated a 4-tweet draft (real `claude -p` call, ~30s). Edited the draft over the API with a marker string — persisted through GET. Marked as posted → `final_text` + `posted_at` stored, entry `state=posted`. Then composed the next generation's prompt for a different item: it contains the edited final text verbatim under "## Voice examples" with voiceCount=1 — the feedback loop demonstrably closes. Copy-all is client-side clipboard (`join("\n\n")`), exercised in the browser pass at the end.

## M5 — YouTube via Floxy (2026-08-12)

**Built:** Real `fetchers/transcript.ts`. Library trial verdict: `youtube-transcript` and `youtubei.js` both wrap the same player-response→timedtext flow but neither supports per-request proxy control, which the fresh-session-per-attempt requirement demands — so the flow is implemented directly with Bun fetch's per-call `proxy` option. Primary path: Innertube WEB player endpoint → caption track list (prefer human English > auto English > first) → timedtext `fmt=json3` → parsed into ~20s blocks with `[mm:ss]` markers; fallback: `captionTracks` scraped off the watch page. 3 attempts, each with a **fresh Floxy session** from `proxy/floxy.ts`, backoff between; a "video has no captions" verdict short-circuits retries. Distinct bot-check detection (`LOGIN_REQUIRED` → "session IP not accepted"). On-match transcript fetch wired into the filter pass (failure recorded in run history and never blocks the Telegram ping); the drafting endpoint retries the fetch when a matched YouTube entry has no transcript. Also fixed a live-caught resolution bug: `@t3dotgg`'s page's first `"channelId"` belonged to a *related* channel ("Theo Rants") — resolution now prefers `externalId`/`og:url` (got the canonical "Theo - t3.gg").

**Verified done-when (as far as possible without proxy credentials):** Real channel added by handle, resolved to the correct channel id, and its 15 current videos detected via the channel RSS feed (initial-import rule applied). Force-matched a video (permissive taste prompt) → run #22's history shows the exact mockup-format attempt trace: "attempt 1 14:09:59 new session ses_04su0m → …" ×3 with a **different session id per attempt**, "giving up after 3 attempts", and the notification still attempted afterwards. Drafting that entry retried the transcript, failed gracefully, and still produced a thread from title+content. timedtext json3 parsing verified against a fixture (produces the mockup's `[mm:ss]` block format).

**Needs real credentials to test:** an end-to-end transcript through a live Floxy session (bot-check clearance from a residential IP, real caption payloads) and "drafted thread references transcript content". This box's datacenter IP is bot-flagged by YouTube (verified: player + watch page both return LOGIN_REQUIRED "Sign in to confirm you're not a bot"), which is precisely the failure mode Floxy exists to clear — the code path is ready and its failure/retry behavior is fully exercised.

## M6 — Twitter & polish (2026-08-12)

**Built:** X-profile flow verified end-to-end as far as the environment allows (fetcher was written in M2: twitter-cli subprocess, env-only credentials, defensive JSON parse, retweet/reply skipping). Sources health column now deep-links "see run" to `/runs?open=<runId>` which auto-expands the failing run; page note shows the live check interval. Inbox cards gained the mockup's meta details (HN points, "video", "transcript fetched"). Runs left unfinished by a process kill are finalized at boot as "interrupted by a server restart" instead of showing "Running…" forever (found via screenshot review). Favicon (crimson serif-C rounded square) has been in since M0. Full visual pass: Playwright screenshots of all five pages in light AND dark against the styleguide/mockups — zero console errors; layout, chips, serif headings, warm-paper palette all match.

**Verified done-when:** Added `@levelsio`; with no CLI installed the run history shows the clean trace ("twitter-cli not found on PATH — install it to fetch X profiles" ×3 attempts) and Sources shows `Failed ×3` with the hint and working see-run link. Expired-cookie mode: with a shim `twitter-cli` that exits 1 printing "HTTP 401 Unauthorized: authentication cookies expired", both "Test auth" and a real run produced "Twitter auth rejected — cookies may have expired. Re-copy them from your browser." Five pages × two themes screenshot-verified.

**Needs real credentials/tools to test:** live tweet ingestion → filter → Telegram → draft end-to-end (requires the actual twitter-cli binary + fresh cookies; the exact subcommand — assumed `twitter-cli tweets <handle> --json` — is pinned in `fetchers/twitter.ts` with a defensive parser and must be confirmed against the real tool). Test-state cleanup: synthetic entries/sources removed, interval restored to 30 min.

## M7 — Cross-source trending (2026-08-13)

**Built:** Two-tier story identity per `spec/plans/trending.md`. `trending/cluster.ts`: `normalizeUrlKey()` (lowercase host, strip `www.`/`utm_*`/`ref`/`fbclid`/`gclid`/`si`/fragment/trailing slash, remaining params sorted) + clustering (join the most recently active cluster in the 48h window on exact `url_key` match OR ≥2 overlapping topic slugs; slug union capped at 8, url_keys at 16; otherwise seed a new cluster) + threshold notification (one `sendTrending` Telegram ping per cluster when it first spans `trending_threshold` distinct sources — stamped `notified_at` only after a successful send, so failed sends retry next run like match pings; taste verdicts are ignored by design; initial-import entries never cluster). Topic slugs ride the existing filter call: `llm/filter.ts` contract is now two lines (MATCH/SKIP + TOPICS with canonical event+entity slugs); a missing TOPICS line never fails the verdict. Migration 3: `clusters`, `cluster_entries`, `entries.topics`/`entries.url_key` (48h backfill of `url_key` at boot), `threads` rebuilt so exactly one of `entry_id`/`cluster_id` is set (CHECK-enforced). Draft-from-cluster: `routes/clusters.ts` + `generateClusterThread()` feed every member's title/content/transcript (transcript budget split across members) into one thread; per-member transcript retry as with entries. UI: Inbox "Trending across your sources" section on the All view (amber 📈 chip, member list with per-source links, skipped-by-filter callout, Draft thread/Open draft/Dismiss), Editor cluster mode at `/cluster/:id` ("The takes" panel listing every member, same draft column), Settings "Trending" card with the N-sources select (2–5, validated 2–9 server-side).

**Verified done-when:** `bun m7-verify.ts` (16 checks, then removed): two sources carrying the same URL (differing only in tracking params) formed exactly one cluster spanning 2 distinct sources and produced exactly one notification attempt listing both — unstamped on failure (no local Telegram creds) and never re-attempted once `notified_at` is set; a taste-SKIPPED member appears in the cluster payload; the topic tier joined two URL-disjoint entries on 2 overlapping slugs; raising the threshold to 3 suppressed the 2-source ping and hid both clusters from the Inbox payload; initial-import and same-source-only entries never notify. Upgrade check (`m7-upgrade-verify.ts`, removed): a simulated v1 DB (user_version 2 with a posted thread) migrated cleanly — thread data intact through the table rebuild, CHECK constraint live, url_key backfilled. Live `claude -p` round-trip returned `{"matched":true,…,"topics":["gpt-6-release","openai","api-pricing"]}` — the two-line contract parses. `bunx tsc --noEmit` clean, `vite build` clean, server boots (migrations 1→3) and serves `/api/entries` with `clusters:[]` + `/api/clusters/:id`. Scratch DB removed after testing.

**Needs real credentials to test:** an actual trending Telegram ping (enter bot token + chat ID in Settings; the send path is the same `send()` used by match pings, which was live-verified in M3's environment).

## M8 — Test suite (2026-08-13)

**Built:** `bun test` in three layers per `spec/plans/testing.md`. Test seams (all useful beyond tests): `CONTENT_ENGINE_DATA_DIR` env override in `db/db.ts` (suite runs on a temp DB via `bunfig.toml` preload), `CONTENT_ENGINE_CLAUDE_BIN` in `llm/claude.ts` (integration tests use a generated stub that sniffs the prompt and answers the filter or generator contract deterministically), `createApp()` extracted to `server/app.ts` (routes with zero boot side effects; `index.ts` is now just boot), `migrateDb(db)` (upgrade test migrates an isolated v1-state database), `parseTweets()` extracted pure from the twitter fetcher, `parseVerdict`/`parseThread` exported. **Unit** (7 files): url-key normalization, filter verdict+topics parsing, thread parsing + transcript truncation, RSS 2.0/Atom fixtures, twitter-cli JSON fixtures, timedtext json3, sourceLabel. **Integration** (5 files): migration idempotency + v1→M7 upgrade intact-thread check, the full `runOnce()` pipeline against a localhost fixture feed (initial-import rule → new-item filter via stub → Telegram attempt recorded → clustering → dedupe on rerun; plus per-source failure isolation with the 3-attempt trace), the M7 clustering matrix (8 cases incl. dismissed clusters and 1-slug-overlap rejection), route contracts (write-only secrets, settings validation, 404s), and the thread studio loop (draft → edit → posted → voice pool → unposted, plus cluster drafts and their member-state neutrality). **Live** (opt-in, `bun run test:live`): owner-edited `test/live-sources.json` drives per-source boundary smoke tests (RSS, YouTube detection feed, twitter-cli with env cookies, HN) plus a real `claude -p` contract round-trip.

**Verified done-when:** `bun run test` — 53 tests, 171 assertions, 0 fail, ~7s, no network/credentials/claude needed. `bun run test:live` with the placeholder config: empty source lists skip cleanly; HN front page (real API) and the real `claude -p` filter round-trip both pass. `tsc --noEmit` clean; server boots and serves after the `createApp()` refactor. The suite caught one real bug while being written: `stripHtml` left double-encoded entities (`&amp;`) as literal text in Atom summaries — fixed in `fetchers/rss.ts`.

## M6 follow-up — Twitter integration pinned against the real CLI (2026-08-20)

**Found:** with twitter-cli actually installed, all three M6 assumptions were wrong: the binary installs as `twitter` (not `twitter-cli`), profile tweets are `twitter user-posts <handle> --json` (not `tweets`), and output is an `{ok, schema_version, data: [...]}` envelope (fields `id`, `text`, `isRetweet`, `author.screenName`, `urls[]`, `quotedTweet`) rather than a bare array.

**Fixed (spec updated first — plans/ingestion.md, plans/trending.md):** `fetchers/twitter.ts` rewritten for the real interface — envelope unwrapping with `ok:false` error surfacing, `whoami --json` for Test auth, retweet skip via `isRetweet`, quoted-tweet text appended to content as filter context, and the tweet's **first embedded URL** exposed as `NewEntry.embedded_url`, which ingestion now prefers when computing the trending `url_key` (resolving trending.md's open question: a tweet linking an article clusters with the HN/RSS entries for that article). Legacy shapes (bare array, `{tweets}`, `id_str`/`full_text`) still parse. Fixture + unit tests rewritten to mirror the real envelope; live test accepts profile URLs, normalized like the Sources route.

**Verified:** `bun run test` 55 pass / 0 fail; `tsc --noEmit` clean. Live with real cookies: `whoami` authenticated (@iamerickdamon), `user-posts` for the configured profile parsed into sane entries — `bun run test:live` 4 pass / 0 fail (YouTube @aiDotEngineer resolution + 15-video detection feed, HN front page, X auth + profile fetch, real `claude -p` contract round-trip).

## M5 follow-up — Floxy session format pinned from a real credential (2026-08-20)

**Found:** the M5 guess (`<username>-session-<id>` in the proxy username) was wrong — a real Floxy credential (`host:port:username:password`) shows rotation is encoded in the **password** as underscore-delimited suffixes: `<password>_session-<id>_lifetime-<seconds>`. Session ids must be purely alphanumeric (the old `ses_xxxxxx` id contained an underscore, which would corrupt Floxy's password parsing).

**Fixed (spec updated first — plans/settings.md):** `proxy/floxy.ts` now builds `password_session-<alnum>_lifetime-300` with a bare username.

**Verified live:** "Test connection" path fetches youtube.com/robots.txt through a fresh session ✓; three consecutive sessions produced three different residential exit IPs (rotation confirmed); Telegram `sendTest` delivered a real message to the owner's chat ✓. Fast suite 55/55, tsc clean.

## Doctor + clear-history (2026-08-20)

**Built:** `bun run doctor` (`doctor.ts`) — dependency & credential checkup with a fix hint per finding: bun + node_modules, SQLite migrations, claude CLI presence AND login (one tiny live `claude -p` call), Telegram token validity via getMe (no message sent), Floxy connectivity through a fresh session, `twitter` binary + cookie auth — including the "cookies in .env.local but not in Settings" trap, called out explicitly since the app only reads Settings. `✕` blockers exit 1; `!` warnings mean one feature degrades. Root `AGENTS.md` (onboarding: run the doctor first on any fresh machine; repo layout; commands; spec-first rules; secrets handling) + `CLAUDE.md` that only imports it.

Also: **Settings → Danger zone → "Clear history data"** (spec'd in plans/settings.md first): POST `/api/settings/clear-history` deletes entries, runs/run_sources, clusters/cluster_entries, and unposted drafts — keeps sources, settings, and posted threads (the voice-example pool). Refused with 409 while a check is running. After clearing, each source's next fetch hits the initial-import rule: current items recorded as seen, no Telegram blast, only genuinely new posts filtered from then on.

**Verified:** doctor run on this machine: correctly green on claude login + twitter binary, correctly warns on unset Telegram/Floxy and the env-vs-Settings cookie split. Integration test covers the clear cycle end-to-end (counts returned, sources + posted thread survive, re-import lands as skipped/initial-import). Suite 56/56, tsc clean, vite build clean.

## Per-run entry detail + live matching test (2026-08-20)

**Live matching test (first real-content filter run):** 8 real entries (5 HN front page + 3 yt:@aiDotEngineer) through the real `claude -p` with the default taste prompt — 1 MATCH (Maven Clinic "AI-Native Health Company" talk: "contrarian, number-rich take on AI-era engineering economics"), 7 skips each with a specific reason (e.g. Go 1.27: "a version bump, no story"), ~9–16s per verdict; the match was delivered to the owner's Telegram for real.

**Built (spec'd in plans/ingestion.md first):** expanding a run on the Runs page now lists **every entry that run touched** — verdict chip (Matched / Skipped / Initial import / Pending), the filter's full reasoning per record, links (matched → editor, skipped → source), and the notification outcome ("sent to Telegram ✓" / "notification not sent yet — see run errors above"). Pending entries explain themselves ("filter didn't reach this entry, re-filtered next run"). Migration 4 adds `entries.created_run_id` + `entries.filtered_run_id` so verdicts are attributable to the run that produced them (leftovers filtered later appear under the later run); initial imports stamp the ingesting run. `GET /api/runs/:id` returns the entry list, matched-first.

**Verified:** pipeline integration test asserts run 1 details (2 × initial import) and run 2 details (1 × matched with the stub's reasoning). Migration count is now asserted via exported SCHEMA_VERSION instead of a hardcoded number. Suite 56/56, tsc clean, vite build clean.
